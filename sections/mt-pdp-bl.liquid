{%- comment -%} MT-PDP-BL — ReversoSkin productpagina sectie {%- endcomment -%}

{%- assign rs_step_badge   = product.metafields.custom.stap_badge -%}
{%- assign rs_expert_naam  = product.metafields.custom.expert_naam -%}
{%- assign rs_expert_quote = product.metafields.custom.expert_quote -%}
{%- assign rs_expert_titel = product.metafields.custom.expert_titel -%}
{%- assign rs_expert_datum = product.metafields.custom.expert_datum -%}
{%- assign rs_expert_foto  = product.metafields.custom.expert_foto -%}
{%- if rs_step_badge == blank -%}{%- assign rs_step_badge = section.settings.badge_fallback -%}{%- endif -%}

{%- assign pkg1_naam     = product.metafields.custom.pkg1_naam -%}
{%- assign pkg1_prijs    = product.metafields.custom.pkg1_prijs -%}
{%- assign pkg1_dagprijs = product.metafields.custom.pkg1_dagprijs -%}
{%- assign pkg1_stuks    = product.metafields.custom.pkg1_stuks -%}
{%- assign pkg1_badge    = product.metafields.custom.pkg1_badge -%}
{%- assign pkg1_foto     = product.metafields.custom.pkg1_foto -%}

{%- assign pkg2_naam     = product.metafields.custom.pkg2_naam -%}
{%- assign pkg2_prijs    = product.metafields.custom.pkg2_prijs -%}
{%- assign pkg2_dagprijs = product.metafields.custom.pkg2_dagprijs -%}
{%- assign pkg2_stuks    = product.metafields.custom.pkg2_stuks -%}
{%- assign pkg2_badge    = product.metafields.custom.pkg2_badge -%}
{%- assign pkg2_foto     = product.metafields.custom.pkg2_foto -%}

{%- assign pkg3_naam     = product.metafields.custom.pkg3_naam -%}
{%- assign pkg3_prijs    = product.metafields.custom.pkg3_prijs -%}
{%- assign pkg3_dagprijs = product.metafields.custom.pkg3_dagprijs -%}
{%- assign pkg3_stuks    = product.metafields.custom.pkg3_stuks -%}
{%- assign pkg3_badge    = product.metafields.custom.pkg3_badge -%}
{%- assign pkg3_foto     = product.metafields.custom.pkg3_foto -%}

{%- assign has_packages = false -%}
{%- if pkg1_naam != blank -%}{%- assign has_packages = true -%}{%- endif -%}

{%- assign cv = product.selected_or_first_available_variant -%}

{%- assign avatar_count = 0 -%}
{%- assign guarantee_count = 0 -%}
{%- for block in section.blocks -%}
  {%- if block.type == 'avatar' -%}{%- assign avatar_count = avatar_count | plus: 1 -%}{%- endif -%}
  {%- if block.type == 'guarantee' -%}{%- assign guarantee_count = guarantee_count | plus: 1 -%}{%- endif -%}
{%- endfor -%}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

{%- style -%}
  #mt-pdp-{{ section.id }} {
    font-family: 'Poppins', -apple-system, sans-serif;
    background: {{ section.settings.section_bg }};
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
  #mt-pdp-{{ section.id }} *, #mt-pdp-{{ section.id }} *::before, #mt-pdp-{{ section.id }} *::after { box-sizing: border-box; margin: 0; padding: 0; }
  #mt-pdp-{{ section.id }} .rs-inner { max-width: 1400px; margin: 0 auto; padding: 0 40px; }
  #mt-pdp-{{ section.id }} .rs-bc { display: flex; align-items: center; flex-wrap: wrap; gap: 6px; padding: 20px 0 28px; }
  #mt-pdp-{{ section.id }} .rs-bc a { font-size: 12px; color: #888; text-decoration: none; }
  #mt-pdp-{{ section.id }} .rs-bc a:hover { color: {{ section.settings.title_color }}; }
  #mt-pdp-{{ section.id }} .rs-bc-sep { font-size: 12px; color: #ccc; }
  #mt-pdp-{{ section.id }} .rs-bc-pill { background: {{ section.settings.badge_bg }}; color: {{ section.settings.badge_fg }}; border-radius: 999px; font-size: 11px; font-weight: 700; padding: 4px 14px; letter-spacing: 0.04em; }
  #mt-pdp-{{ section.id }} .rs-grid { display: grid; grid-template-columns: 72px 1fr 1fr; gap: 12px 32px; align-items: start; }
  #mt-pdp-{{ section.id }} .rs-thumbs { display: flex; flex-direction: column; gap: 10px; grid-column: 1; grid-row: 1; }
  #mt-pdp-{{ section.id }} .rs-thumb { width: 72px; aspect-ratio: 1; background: {{ section.settings.image_bg }}; border-radius: 10px; overflow: hidden; cursor: pointer; border: 1.5px solid transparent; transition: border-color 0.15s; flex-shrink: 0; }
  #mt-pdp-{{ section.id }} .rs-thumb:hover, #mt-pdp-{{ section.id }} .rs-thumb.on { border-color: {{ section.settings.accent_color }}; }
  #mt-pdp-{{ section.id }} .rs-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
  #mt-pdp-{{ section.id }} .rs-img-main { grid-column: 2; grid-row: 1; width: 100%; aspect-ratio: 1/1; background: {{ section.settings.image_bg }}; border-radius: 20px; overflow: hidden; display: flex; align-items: center; justify-content: center; }
  #mt-pdp-{{ section.id }} .rs-img-main img { width: 100%; height: 100%; object-fit: contain; display: block; }
  #mt-pdp-{{ section.id }} .rs-info { grid-column: 3; grid-row: 1; position: sticky; top: 90px; }
  #mt-pdp-{{ section.id }} .rs-badge { display: inline-block; background: {{ section.settings.badge_bg }}; color: {{ section.settings.badge_fg }}; border-radius: 999px; font-size: 11px; font-weight: 700; letter-spacing: 0.06em; padding: 5px 16px; margin-bottom: 16px; }
  #mt-pdp-{{ section.id }} .rs-title { font-family: 'Poppins', sans-serif; font-size: {{ section.settings.title_size }}px; font-weight: 700; line-height: 1.15; letter-spacing: -0.01em; color: {{ section.settings.title_color }}; margin-bottom: 14px; }
  #mt-pdp-{{ section.id }} .rs-rating { display: flex; align-items: center; gap: 8px; margin-bottom: 16px; }
  #mt-pdp-{{ section.id }} .rs-stars { color: {{ section.settings.accent_color }}; font-size: 14px; letter-spacing: 1px; }
  #mt-pdp-{{ section.id }} .rs-rating-txt { font-size: 13px; color: #888; }
  #mt-pdp-{{ section.id }} .rs-price { font-size: {{ section.settings.price_size }}px; font-weight: 700; color: {{ section.settings.price_color }}; margin-bottom: 20px; }
  #mt-pdp-{{ section.id }} .rs-var-label { font-size: 10px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: #888; margin-bottom: 10px; }
  #mt-pdp-{{ section.id }} .rs-vars { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 22px; }
  #mt-pdp-{{ section.id }} .rs-var { padding: 10px 18px; border: 1.5px solid #e0e0e0; border-radius: 999px; font-size: 13px; font-family: 'Poppins', sans-serif; color: {{ section.settings.title_color }}; background: {{ section.settings.section_bg }}; cursor: pointer; transition: all 0.15s; }
  #mt-pdp-{{ section.id }} .rs-var:hover { border-color: {{ section.settings.title_color }}; }
  #mt-pdp-{{ section.id }} .rs-var.on { border-color: {{ section.settings.title_color }}; background: {{ section.settings.title_color }}; color: {{ section.settings.section_bg }}; font-weight: 600; }
  #mt-pdp-{{ section.id }} .rs-var:disabled { opacity: 0.35; cursor: not-allowed; }
  #mt-pdp-{{ section.id }} .rs-buy { display: block; margin-bottom: 14px; }
  #mt-pdp-{{ section.id }} .rs-atc { width: 100%; height: 56px; min-height: 56px; background: {{ section.settings.button_bg }}; color: {{ section.settings.button_color }}; border: none; border-radius: 999px; font-size: 16px; font-weight: 700; font-family: 'Poppins', sans-serif; cursor: pointer; transition: filter 0.15s; display: flex; align-items: center; justify-content: center; white-space: nowrap; }
  #mt-pdp-{{ section.id }} .rs-atc:hover:not(:disabled) { filter: brightness(0.85); }
  #mt-pdp-{{ section.id }} .rs-delivery { font-size: 12px; color: #888; margin-bottom: 22px; line-height: 1.5; }
  #mt-pdp-{{ section.id }} .rs-bens { display: flex; flex-direction: column; gap: 11px; padding: 18px 0; }
  #mt-pdp-{{ section.id }} .rs-ben { display: flex; align-items: center; gap: 11px; }
  #mt-pdp-{{ section.id }} .rs-check { width: 20px; height: 20px; border-radius: 50%; background: #fff3e8; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
  #mt-pdp-{{ section.id }} .rs-check svg { width: 10px; height: 10px; stroke: {{ section.settings.accent_color }}; stroke-width: 2.5; fill: none; stroke-linecap: round; stroke-linejoin: round; }
  #mt-pdp-{{ section.id }} .rs-ben-txt { font-size: 14px; color: {{ section.settings.text_color }}; line-height: 1.4; }
  #mt-pdp-{{ section.id }} .rs-accordion { border-top: 0.5px solid #eee; }
  #mt-pdp-{{ section.id }} .rs-acc-item { border-bottom: 0.5px solid #eee; }
  #mt-pdp-{{ section.id }} .rs-acc-q { display: flex; justify-content: space-between; align-items: center; padding: 16px 0; cursor: pointer; gap: 16px; }
  #mt-pdp-{{ section.id }} .rs-acc-q-txt { font-size: 13px; font-weight: 700; font-family: 'Poppins', sans-serif; letter-spacing: 0.04em; text-transform: uppercase; color: {{ section.settings.title_color }}; }
  #mt-pdp-{{ section.id }} .rs-acc-icon { font-size: 20px; font-weight: 300; color: {{ section.settings.title_color }}; flex-shrink: 0; line-height: 1; user-select: none; }
  #mt-pdp-{{ section.id }} .rs-acc-body { display: none; padding-bottom: 18px; }
  #mt-pdp-{{ section.id }} .rs-acc-body.open { display: block; }
  #mt-pdp-{{ section.id }} .rs-acc-body p { font-size: 14px; font-family: 'Poppins', sans-serif; color: #555; line-height: 1.8; margin-bottom: 10px; }
  #mt-pdp-{{ section.id }} .rs-acc-body p:last-child { margin-bottom: 0; }
  #mt-pdp-{{ section.id }} .rs-acc-body strong { font-weight: 700; color: {{ section.settings.title_color }}; }
  #mt-pdp-{{ section.id }} .rs-acc-body em { font-style: italic; }
  #mt-pdp-{{ section.id }} .rs-acc-body ul, #mt-pdp-{{ section.id }} .rs-acc-body ol { padding-left: 20px; margin-bottom: 10px; }
  #mt-pdp-{{ section.id }} .rs-acc-body li { font-size: 14px; font-family: 'Poppins', sans-serif; color: #555; line-height: 1.8; margin-bottom: 4px; }
  #mt-pdp-{{ section.id }} .rs-acc-body h2 { font-family: 'Poppins', sans-serif; font-size: 15px; font-weight: 700; color: {{ section.settings.title_color }}; margin-bottom: 8px; margin-top: 12px; }
  #mt-pdp-{{ section.id }} .rs-acc-body h3 { font-family: 'Poppins', sans-serif; font-size: 14px; font-weight: 700; color: {{ section.settings.title_color }}; margin-bottom: 6px; margin-top: 10px; }
  #mt-pdp-{{ section.id }} .rs-acc-body a { color: {{ section.settings.accent_color }}; text-decoration: underline; }
  #mt-pdp-{{ section.id }} .rs-expert { margin-top: 28px; padding: 20px; background: #f9f9f7; border-radius: 16px; border: 0.5px solid #eee; }
  #mt-pdp-{{ section.id }} .rs-expert-stars { color: #00B77F; font-size: 14px; letter-spacing: 1px; margin-bottom: 12px; display: block; }
  #mt-pdp-{{ section.id }} .rs-expert-quote { font-size: 14px; font-family: 'Poppins', sans-serif; color: #333; line-height: 1.8; margin-bottom: 16px; }
  #mt-pdp-{{ section.id }} .rs-expert-author { display: flex; align-items: center; gap: 12px; }
  #mt-pdp-{{ section.id }} .rs-expert-avatar { width: 44px; height: 44px; border-radius: 50%; object-fit: cover; flex-shrink: 0; background: #eee; }
  #mt-pdp-{{ section.id }} .rs-expert-name { font-size: 13px; font-weight: 700; font-family: 'Poppins', sans-serif; color: {{ section.settings.title_color }}; display: flex; align-items: center; gap: 6px; flex-wrap: wrap; }
  #mt-pdp-{{ section.id }} .rs-expert-verified { font-size: 10px; font-weight: 600; color: #0a7a4b; background: #e6f4ee; border-radius: 999px; padding: 2px 8px; }
  #mt-pdp-{{ section.id }} .rs-expert-meta { font-size: 11px; color: #888; margin-top: 2px; font-family: 'Poppins', sans-serif; }

  /* ── Avatar row ── */
  #mt-pdp-{{ section.id }} .rs-avatars { display: flex; align-items: center; gap: 10px; margin-bottom: 16px; }
  #mt-pdp-{{ section.id }} .rs-avatar-stack { display: flex; align-items: center; }
  #mt-pdp-{{ section.id }} .rs-avatar-img { width: 32px; height: 32px; border-radius: 50%; object-fit: cover; border: 2px solid {{ section.settings.section_bg }}; margin-left: -8px; flex-shrink: 0; background: #eee; }
  #mt-pdp-{{ section.id }} .rs-avatar-img:first-child { margin-left: 0; }
  #mt-pdp-{{ section.id }} .rs-avatar-rating { display: flex; align-items: center; gap: 6px; }
  #mt-pdp-{{ section.id }} .rs-avatar-stars { color: {{ section.settings.accent_color }}; font-size: 15px; letter-spacing: 1px; }
  #mt-pdp-{{ section.id }} .rs-avatar-score { font-size: 14px; font-weight: 700; color: {{ section.settings.title_color }}; }
  #mt-pdp-{{ section.id }} .rs-avatar-count { font-size: 12px; color: #888; }

  /* ── Package cards ── */
  #mt-pdp-{{ section.id }} .rs-pkg-wrap { margin-bottom: 20px; }
  #mt-pdp-{{ section.id }} .rs-pkg-label { font-size: 10px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: #888; margin-bottom: 10px; }
  #mt-pdp-{{ section.id }} .rs-pkg-card { border: 1.5px solid #e0e0e0; border-radius: 14px; padding: 14px; margin-bottom: 8px; cursor: pointer; position: relative; background: #ffffff; transition: border-color 0.15s; }
  #mt-pdp-{{ section.id }} .rs-pkg-card:hover { border-color: #999; }
  #mt-pdp-{{ section.id }} .rs-pkg-card.rs-pkg-selected { border-color: {{ section.settings.title_color }}; }
  #mt-pdp-{{ section.id }} .rs-pkg-badge { position: absolute; top: -10px; right: 12px; border-radius: 999px; font-size: 9px; font-weight: 700; padding: 3px 10px; letter-spacing: 0.06em; text-transform: uppercase; }
  #mt-pdp-{{ section.id }} .rs-pkg-top { display: flex; align-items: center; gap: 12px; }
  #mt-pdp-{{ section.id }} .rs-pkg-img { width: 44px; height: 44px; border-radius: 8px; background: {{ section.settings.image_bg }}; flex-shrink: 0; overflow: hidden; display: flex; align-items: center; justify-content: center; }
  #mt-pdp-{{ section.id }} .rs-pkg-img img { width: 100%; height: 100%; object-fit: contain; }
  #mt-pdp-{{ section.id }} .rs-pkg-info { flex: 1; min-width: 0; }
  #mt-pdp-{{ section.id }} .rs-pkg-name { font-size: 13px; font-weight: 700; color: {{ section.settings.title_color }}; line-height: 1.25; }
  #mt-pdp-{{ section.id }} .rs-pkg-day { font-size: 10px; color: #888; margin-top: 2px; }
  #mt-pdp-{{ section.id }} .rs-pkg-price { text-align: right; flex-shrink: 0; }
  #mt-pdp-{{ section.id }} .rs-pkg-price-cur { font-size: 15px; font-weight: 700; color: {{ section.settings.title_color }}; display: block; }
  #mt-pdp-{{ section.id }} .rs-pkg-price-day { font-size: 10px; color: #888; display: block; text-align: right; margin-top: 2px; }

  /* ── Guarantee ── */
  #mt-pdp-{{ section.id }} .rs-guarantee { display: flex; align-items: center; gap: 14px; padding: 14px 16px; border: 0.5px solid #eee; margin-bottom: 20px; }
  #mt-pdp-{{ section.id }} .rs-guarantee-icon { width: 56px; height: 56px; border-radius: 50%; flex-shrink: 0; display: flex; align-items: center; justify-content: center; overflow: hidden; border: 1.5px solid #e0e0e0; }
  #mt-pdp-{{ section.id }} .rs-guarantee-icon img { width: 100%; height: 100%; object-fit: contain; }
  #mt-pdp-{{ section.id }} .rs-guarantee-title { font-size: 13px; font-weight: 700; color: {{ section.settings.title_color }}; margin-bottom: 3px; }
  #mt-pdp-{{ section.id }} .rs-guarantee-body { font-size: 11px; color: #555; line-height: 1.55; margin-bottom: 3px; }
  #mt-pdp-{{ section.id }} .rs-guarantee-sub { font-size: 10px; color: #aaa; }

  @media (max-width: 900px) {
    #mt-pdp-{{ section.id }} .rs-inner { padding: 0 20px; }
    #mt-pdp-{{ section.id }} .rs-grid { grid-template-columns: 1fr; grid-template-rows: auto auto auto; gap: 16px; }
    #mt-pdp-{{ section.id }} .rs-img-main { grid-column: 1; grid-row: 1; }
    #mt-pdp-{{ section.id }} .rs-thumbs { grid-column: 1; grid-row: 2; flex-direction: row; overflow-x: auto; gap: 8px; width: 100%; scrollbar-width: none; }
    #mt-pdp-{{ section.id }} .rs-thumbs::-webkit-scrollbar { display: none; }
    #mt-pdp-{{ section.id }} .rs-thumb { width: 64px; flex-shrink: 0; }
    #mt-pdp-{{ section.id }} .rs-info { grid-column: 1; grid-row: 3; position: static; }
    #mt-pdp-{{ section.id }} .rs-title { font-size: {{ section.settings.title_size_mobile }}px; }
    #mt-pdp-{{ section.id }} .rs-price { font-size: {{ section.settings.price_size_mobile }}px; }
  }
{%- endstyle -%}

<div id="mt-pdp-{{ section.id }}">
  <div class="rs-inner">

    <nav class="rs-bc">
      <a href="/">Home</a>
      <span class="rs-bc-sep">/</span>
      <a href="/collections/all">Collectie</a>
      <span class="rs-bc-sep">/</span>
      <span class="rs-bc-pill">{{ rs_step_badge }}</span>
    </nav>

    <div class="rs-grid">

      <div class="rs-thumbs">
        {%- for img in product.images limit: section.settings.max_images -%}
          <div class="rs-thumb{%- if forloop.first %} on{%- endif -%}"
               onclick="rsPdpPic_{{ section.id }}('{{ img | image_url: width: 1000 }}', this)">
            <img src="{{ img | image_url: width: 150 }}" alt="{{ img.alt | escape }}" loading="lazy">
          </div>
        {%- endfor -%}
      </div>

      <div class="rs-img-main">
        {%- if product.featured_image -%}
          <img id="rs-img-{{ section.id }}"
               src="{{ product.featured_image | image_url: width: 1000 }}"
               alt="{{ product.featured_image.alt | escape }}"
               loading="eager">
        {%- endif -%}
      </div>

      <div class="rs-info">

        <!-- 1. Avatar row -->
        {%- if avatar_count > 0 -%}
          <div class="rs-avatars">
            <div class="rs-avatar-stack">
              {%- for block in section.blocks -%}
                {%- if block.type == 'avatar' -%}
                  {%- if block.settings.avatar_image -%}
                    <img class="rs-avatar-img"
                         src="{{ block.settings.avatar_image | image_url: width: 64 }}"
                         alt="{{ block.settings.avatar_alt | escape }}"
                         {{ block.shopify_attributes }}>
                  {%- else -%}
                    <div class="rs-avatar-img" {{ block.shopify_attributes }}></div>
                  {%- endif -%}
                {%- endif -%}
              {%- endfor -%}
            </div>
            <div class="rs-avatar-rating">
              <span class="rs-avatar-stars">
                {%- assign full_stars = section.settings.rating_stars -%}
                {%- if full_stars > 0 -%}{%- for i in (1..full_stars) -%}★{%- endfor -%}{%- endif -%}
              </span>
              <span class="rs-avatar-score">{{ section.settings.rating_score }}/5</span>
              <span class="rs-avatar-count">({{ section.settings.rating_count }} reviews)</span>
            </div>
          </div>
        {%- endif -%}

        <!-- 2. Badge -->
        <span class="rs-badge">{{ rs_step_badge }}</span>

        <!-- 3. Title -->
        <h1 class="rs-title">{{ product.title }}</h1>

        <!-- 4. Rating (only if no avatar blocks) -->
        {%- if avatar_count == 0 -%}
          <div class="rs-rating">
            <span class="rs-stars">
              {%- assign full_stars = section.settings.rating_stars -%}
              {%- assign empty_stars = 5 | minus: full_stars -%}
              {%- if full_stars > 0 -%}{%- for i in (1..full_stars) -%}★{%- endfor -%}{%- endif -%}
              {%- if empty_stars > 0 -%}{%- for i in (1..empty_stars) -%}☆{%- endfor -%}{%- endif -%}
            </span>
            <span class="rs-rating-txt">{{ section.settings.rating_score }}/5 &middot; {{ section.settings.rating_count }} reviews</span>
          </div>
        {%- endif -%}

        <!-- 5. Price -->
        <div class="rs-price" id="rs-price-{{ section.id }}">{{ cv.price | money }}</div>

        <!-- 6. Package cards (metafields) OR normal variant pills -->
        {%- if has_packages -%}
          <div class="rs-pkg-wrap">
            <p class="rs-pkg-label">Kies jouw pakket</p>

            {%- comment -%} PAKKET 1 {%- endcomment -%}
            {%- if pkg1_naam != blank -%}
              <div class="rs-pkg-card rs-pkg-selected"
                   data-qty="{{ pkg1_stuks | default: 1 }}"
                   data-price="{{ pkg1_prijs }}"
                   data-vid="{{ product.selected_or_first_available_variant.id }}"
                   onclick="window['rsPdpSelectPkg_{{ section.id }}'](this)">
                {%- if pkg1_badge != blank -%}
                  <span class="rs-pkg-badge"
                        style="background: {{ section.settings.badge_bg }}; color: {{ section.settings.badge_fg }};">{{ pkg1_badge }}</span>
                {%- endif -%}
                <div class="rs-pkg-top">
                  <div class="rs-pkg-img">
                    {%- if pkg1_foto != blank -%}
                      <img src="{{ pkg1_foto | image_url: width: 88 }}" alt="{{ pkg1_naam }}">
                    {%- elsif product.featured_image -%}
                      <img src="{{ product.featured_image | image_url: width: 88 }}" alt="{{ pkg1_naam }}">
                    {%- endif -%}
                  </div>
                  <div class="rs-pkg-info">
                    <div class="rs-pkg-name">{{ pkg1_naam }}</div>
                    {%- if pkg1_dagprijs != blank -%}
                      <div class="rs-pkg-day">Slechts {{ pkg1_dagprijs }} per dag</div>
                    {%- endif -%}
                  </div>
                  <div class="rs-pkg-price">
                    <span class="rs-pkg-price-cur">{{ pkg1_prijs }}</span>
                    {%- if pkg1_dagprijs != blank -%}
                      <span class="rs-pkg-price-day">{{ pkg1_dagprijs }}/dag</span>
                    {%- endif -%}
                  </div>
                </div>
              </div>
            {%- endif -%}

            {%- comment -%} PAKKET 2 {%- endcomment -%}
            {%- if pkg2_naam != blank -%}
              <div class="rs-pkg-card"
                   data-qty="{{ pkg2_stuks | default: 1 }}"
                   data-price="{{ pkg2_prijs }}"
                   data-vid="{{ product.selected_or_first_available_variant.id }}"
                   onclick="window['rsPdpSelectPkg_{{ section.id }}'](this)">
                {%- if pkg2_badge != blank -%}
                  <span class="rs-pkg-badge"
                        style="background: {{ section.settings.badge_bg }}; color: {{ section.settings.badge_fg }};">{{ pkg2_badge }}</span>
                {%- endif -%}
                <div class="rs-pkg-top">
                  <div class="rs-pkg-img">
                    {%- if pkg2_foto != blank -%}
                      <img src="{{ pkg2_foto | image_url: width: 88 }}" alt="{{ pkg2_naam }}">
                    {%- elsif product.featured_image -%}
                      <img src="{{ product.featured_image | image_url: width: 88 }}" alt="{{ pkg2_naam }}">
                    {%- endif -%}
                  </div>
                  <div class="rs-pkg-info">
                    <div class="rs-pkg-name">{{ pkg2_naam }}</div>
                    {%- if pkg2_dagprijs != blank -%}
                      <div class="rs-pkg-day">Slechts {{ pkg2_dagprijs }} per dag</div>
                    {%- endif -%}
                  </div>
                  <div class="rs-pkg-price">
                    <span class="rs-pkg-price-cur">{{ pkg2_prijs }}</span>
                    {%- if pkg2_dagprijs != blank -%}
                      <span class="rs-pkg-price-day">{{ pkg2_dagprijs }}/dag</span>
                    {%- endif -%}
                  </div>
                </div>
              </div>
            {%- endif -%}

            {%- comment -%} PAKKET 3 {%- endcomment -%}
            {%- if pkg3_naam != blank -%}
              <div class="rs-pkg-card"
                   data-qty="{{ pkg3_stuks | default: 1 }}"
                   data-price="{{ pkg3_prijs }}"
                   data-vid="{{ product.selected_or_first_available_variant.id }}"
                   onclick="window['rsPdpSelectPkg_{{ section.id }}'](this)">
                {%- if pkg3_badge != blank -%}
                  <span class="rs-pkg-badge"
                        style="background: {{ section.settings.badge_bg }}; color: {{ section.settings.badge_fg }};">{{ pkg3_badge }}</span>
                {%- endif -%}
                <div class="rs-pkg-top">
                  <div class="rs-pkg-img">
                    {%- if pkg3_foto != blank -%}
                      <img src="{{ pkg3_foto | image_url: width: 88 }}" alt="{{ pkg3_naam }}">
                    {%- elsif product.featured_image -%}
                      <img src="{{ product.featured_image | image_url: width: 88 }}" alt="{{ pkg3_naam }}">
                    {%- endif -%}
                  </div>
                  <div class="rs-pkg-info">
                    <div class="rs-pkg-name">{{ pkg3_naam }}</div>
                    {%- if pkg3_dagprijs != blank -%}
                      <div class="rs-pkg-day">Slechts {{ pkg3_dagprijs }} per dag</div>
                    {%- endif -%}
                  </div>
                  <div class="rs-pkg-price">
                    <span class="rs-pkg-price-cur">{{ pkg3_prijs }}</span>
                    {%- if pkg3_dagprijs != blank -%}
                      <span class="rs-pkg-price-day">{{ pkg3_dagprijs }}/dag</span>
                    {%- endif -%}
                  </div>
                </div>
              </div>
            {%- endif -%}

          </div>
        {%- else -%}
          {%- if product.variants.size > 1 -%}
            <p class="rs-var-label">Formaat</p>
            <div class="rs-vars">
              {%- for v in product.variants -%}
                <button class="rs-var{%- if v == cv %} on{%- endif -%}"
                        data-id="{{ v.id }}"
                        data-price="{{ v.price | money }}"
                        onclick="rsPdpVar_{{ section.id }}(this)"
                        {%- unless v.available %} disabled{%- endunless -%}>
                  {{ v.title }} &middot; {{ v.price | money }}
                </button>
              {%- endfor -%}
            </div>
          {%- endif -%}
        {%- endif -%}

        <input type="hidden" id="rs-vid-{{ section.id }}" value="{{ cv.id }}">

        <!-- 7. ATC button -->
        <div class="rs-buy">
          <button class="rs-atc"
                  id="rs-atc-{{ section.id }}"
                  type="button"
                  data-accent="{{ section.settings.accent_color }}"
                  onclick="rsPdpAdd_{{ section.id }}()">In winkelmand</button>
        </div>

        <!-- 8. Delivery text -->
        <p class="rs-delivery">{{ section.settings.delivery_text }}</p>

        <!-- 9. Guarantee blocks -->
        {%- if guarantee_count > 0 -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'guarantee' -%}
              <div class="rs-guarantee"
                   style="background: {{ block.settings.guarantee_bg }}; border-radius: {{ block.settings.guarantee_radius }}px;"
                   {{ block.shopify_attributes }}>
                <div class="rs-guarantee-icon">
                  {%- if block.settings.guarantee_icon_image -%}
                    <img src="{{ block.settings.guarantee_icon_image | image_url: width: 112 }}"
                         alt="{{ block.settings.guarantee_title | escape }}">
                  {%- endif -%}
                </div>
                <div>
                  <div class="rs-guarantee-title">{{ block.settings.guarantee_title | escape }}</div>
                  <div class="rs-guarantee-body">{{ block.settings.guarantee_body | escape }}</div>
                  {%- if block.settings.guarantee_sub != blank -%}
                    <div class="rs-guarantee-sub">{{ block.settings.guarantee_sub | escape }}</div>
                  {%- endif -%}
                </div>
              </div>
            {%- endif -%}
          {%- endfor -%}
        {%- endif -%}

        <!-- 10. Benefits -->
        {%- assign benefit_count = 0 -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'benefit' -%}{%- assign benefit_count = benefit_count | plus: 1 -%}{%- endif -%}
        {%- endfor -%}
        {%- if benefit_count > 0 -%}
          <div class="rs-bens">
            {%- for block in section.blocks -%}
              {%- if block.type == 'benefit' -%}
                <div class="rs-ben" {{ block.shopify_attributes }}>
                  <div class="rs-check"><svg viewBox="0 0 12 12"><polyline points="2,6 5,9 10,3"></polyline></svg></div>
                  <span class="rs-ben-txt">{{ block.settings.text }}</span>
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}

        <!-- 11. Accordion tabs -->
        <div class="rs-accordion">
          {%- if product.metafields.custom.tab_beschrijving != blank -%}
            <div class="rs-acc-item">
              <div class="rs-acc-q" onclick="rsPdpAcc(this)">
                <span class="rs-acc-q-txt">{{ section.settings.tab_beschrijving_label }}</span>
                <span class="rs-acc-icon">+</span>
              </div>
              <div class="rs-acc-body">{{ product.metafields.custom.tab_beschrijving | metafield_tag }}</div>
            </div>
          {%- endif -%}
          {%- if product.metafields.custom.tab_ingredienten != blank -%}
            <div class="rs-acc-item">
              <div class="rs-acc-q" onclick="rsPdpAcc(this)">
                <span class="rs-acc-q-txt">{{ section.settings.tab_ingredienten_label }}</span>
                <span class="rs-acc-icon">+</span>
              </div>
              <div class="rs-acc-body">{{ product.metafields.custom.tab_ingredienten | metafield_tag }}</div>
            </div>
          {%- endif -%}
          {%- if product.metafields.custom.tab_waarom != blank -%}
            <div class="rs-acc-item">
              <div class="rs-acc-q" onclick="rsPdpAcc(this)">
                <span class="rs-acc-q-txt">{{ section.settings.tab_waarom_label }}</span>
                <span class="rs-acc-icon">+</span>
              </div>
              <div class="rs-acc-body">{{ product.metafields.custom.tab_waarom | metafield_tag }}</div>
            </div>
          {%- endif -%}
          {%- if product.metafields.custom.tab_gebruik != blank -%}
            <div class="rs-acc-item">
              <div class="rs-acc-q" onclick="rsPdpAcc(this)">
                <span class="rs-acc-q-txt">{{ section.settings.tab_gebruik_label }}</span>
                <span class="rs-acc-icon">+</span>
              </div>
              <div class="rs-acc-body">{{ product.metafields.custom.tab_gebruik | metafield_tag }}</div>
            </div>
          {%- endif -%}
        </div>

        <!-- 12. Expert review -->
        {%- if rs_expert_quote != blank -%}
          <div class="rs-expert">
            <span class="rs-expert-stars">★★★★★</span>
            <p class="rs-expert-quote">{{ rs_expert_quote }}</p>
            <div class="rs-expert-author">
              {%- if rs_expert_foto != blank -%}
                <img class="rs-expert-avatar" src="{{ rs_expert_foto | image_url: width: 100 }}" alt="{{ rs_expert_naam }}">
              {%- else -%}
                <div class="rs-expert-avatar"></div>
              {%- endif -%}
              <div>
                <div class="rs-expert-name">
                  {{ rs_expert_naam }}
                  <span class="rs-expert-verified">✓ Verified Expert</span>
                </div>
                <div class="rs-expert-meta">{{ rs_expert_titel }} &middot; {{ rs_expert_datum }}</div>
              </div>
            </div>
          </div>
        {%- endif -%}

      </div>
    </div>
  </div>
</div>

<script>
(function () {
  var sid = '{{ section.id }}';

  window['rsPdpPic_' + sid] = function (src, el) {
    var img = document.getElementById('rs-img-' + sid);
    if (img) img.src = src;
    document.getElementById('mt-pdp-' + sid).querySelectorAll('.rs-thumb').forEach(function (t) { t.classList.remove('on'); });
    el.classList.add('on');
  };

  window['rsPdpVar_' + sid] = function (btn) {
    document.getElementById('mt-pdp-' + sid).querySelectorAll('.rs-var').forEach(function (b) { b.classList.remove('on'); });
    btn.classList.add('on');
    document.getElementById('rs-vid-' + sid).value = btn.dataset.id;
    document.getElementById('rs-price-' + sid).textContent = btn.dataset.price;
  };

  window['rsPdpAdd_' + sid] = function () {
    var btn = document.getElementById('rs-atc-' + sid);
    var vid = parseInt(document.getElementById('rs-vid-' + sid).value);
    var selectedPkg = document.querySelector('#mt-pdp-' + sid + ' .rs-pkg-card.rs-pkg-selected');
    var qty = selectedPkg ? parseInt(selectedPkg.dataset.qty || 1) : 1;
    console.log('Adding to cart — vid:', vid, 'qty:', qty);
    if (!vid) return;
    btn.disabled = true;
    btn.textContent = 'Bezig…';
    fetch('/cart/add.js', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ id: vid, quantity: qty })
    })
    .then(function (r) {
      if (!r.ok) throw new Error('fout');
      btn.textContent = 'Toegevoegd ✓';
      btn.style.background = btn.dataset.accent;
      document.dispatchEvent(new CustomEvent('cart:refresh'));
      setTimeout(function () { btn.textContent = 'In winkelmand'; btn.style.background = ''; btn.disabled = false; }, 2000);
    })
    .catch(function () {
      btn.textContent = 'Probeer opnieuw';
      btn.style.background = '#c0392b';
      btn.disabled = false;
      setTimeout(function () { btn.textContent = 'In winkelmand'; btn.style.background = ''; }, 2500);
    });
  };

  window['rsPdpSelectPkg_' + sid] = function (card) {
    document.querySelectorAll('#mt-pdp-' + sid + ' .rs-pkg-card').forEach(function (c) {
      c.classList.remove('rs-pkg-selected');
    });
    card.classList.add('rs-pkg-selected');
    var priceEl = document.getElementById('rs-price-' + sid);
    if (priceEl && card.dataset.price) priceEl.textContent = card.dataset.price;
    var vidEl = document.getElementById('rs-vid-' + sid);
    if (vidEl && card.dataset.vid) vidEl.value = card.dataset.vid;
  };

  document.addEventListener('DOMContentLoaded', function () {
    var firstPkg = document.querySelector('#mt-pdp-' + sid + ' .rs-pkg-card');
    if (firstPkg) firstPkg.classList.add('rs-pkg-selected');
    var vidEl = document.getElementById('rs-vid-' + sid);
    if (vidEl) vidEl.value = firstPkg ? firstPkg.dataset.vid : '{{ product.selected_or_first_available_variant.id }}';
  });
})();

function rsPdpAcc(header) {
  var body = header.nextElementSibling;
  var icon = header.querySelector('.rs-acc-icon');
  var isOpen = body.classList.contains('open');
  body.classList.toggle('open');
  icon.textContent = isOpen ? '+' : '-';
}
</script>

{% schema %}
{
  "name": "MT-PDP-BL",
  "settings": [
    { "type": "header", "content": "Sectie" },
    { "type": "range", "id": "padding_top", "min": 0, "max": 120, "step": 8, "unit": "px", "label": "Padding boven", "default": 0 },
    { "type": "range", "id": "padding_bottom", "min": 0, "max": 120, "step": 8, "unit": "px", "label": "Padding onder", "default": 80 },
    { "type": "color", "id": "section_bg", "label": "Sectie achtergrond", "default": "#ffffff" },
    { "type": "header", "content": "Badge" },
    { "type": "text", "id": "badge_fallback", "label": "Badge tekst (fallback)", "info": "Wordt gebruikt als het metaveld 'stap_badge' leeg is.", "default": "Stap 1" },
    { "type": "color", "id": "badge_bg", "label": "Badge achtergrond", "default": "#ff7900" },
    { "type": "color", "id": "badge_fg", "label": "Badge tekstkleur", "default": "#ffffff" },
    { "type": "header", "content": "Reviews" },
    { "type": "text", "id": "rating_score", "label": "Score (bijv. 4,7)", "default": "4,7" },
    { "type": "text", "id": "rating_count", "label": "Aantal reviews", "default": "412" },
    { "type": "range", "id": "rating_stars", "min": 0, "max": 5, "step": 1, "label": "Sterren (0–5)", "default": 4 },
    { "type": "header", "content": "Verzending" },
    { "type": "textarea", "id": "delivery_text", "label": "Bezorgtekst", "default": "Voor 15:00 besteld, morgen in huis · Gratis verzending vanaf €50" },
    { "type": "header", "content": "Afbeeldingen" },
    { "type": "range", "id": "max_images", "min": 1, "max": 10, "step": 1, "label": "Max. thumbnails", "default": 6 },
    { "type": "color", "id": "image_bg", "label": "Afbeelding achtergrond", "default": "#f5f4f0" },
    { "type": "header", "content": "Typografie" },
    { "type": "range", "id": "title_size", "min": 20, "max": 56, "step": 2, "unit": "px", "label": "Titel grootte (desktop)", "default": 34 },
    { "type": "range", "id": "title_size_mobile", "min": 18, "max": 40, "step": 2, "unit": "px", "label": "Titel grootte (mobiel)", "default": 26 },
    { "type": "range", "id": "price_size", "min": 18, "max": 48, "step": 2, "unit": "px", "label": "Prijs grootte (desktop)", "default": 30 },
    { "type": "range", "id": "price_size_mobile", "min": 16, "max": 36, "step": 2, "unit": "px", "label": "Prijs grootte (mobiel)", "default": 24 },
    { "type": "header", "content": "Kleuren" },
    { "type": "color", "id": "accent_color", "label": "Accentkleur (sterren, check, badge)", "default": "#ff7900" },
    { "type": "color", "id": "title_color", "label": "Titel / tekst kleur", "default": "#111111" },
    { "type": "color", "id": "price_color", "label": "Prijs kleur", "default": "#111111" },
    { "type": "color", "id": "text_color", "label": "Voordelen tekst kleur", "default": "#444444" },
    { "type": "color", "id": "button_bg", "label": "Knop achtergrond", "default": "#111111" },
    { "type": "color", "id": "button_color", "label": "Knop tekstkleur", "default": "#ffffff" },
    { "type": "header", "content": "Accordeon labels" },
    { "type": "text", "id": "tab_beschrijving_label", "label": "Tab: Beschrijving", "default": "Beschrijving" },
    { "type": "text", "id": "tab_ingredienten_label", "label": "Tab: Ingrediënten", "default": "Ingrediënten" },
    { "type": "text", "id": "tab_waarom_label", "label": "Tab: Waarom Reverso", "default": "Waarom Reverso" },
    { "type": "text", "id": "tab_gebruik_label", "label": "Tab: Hoe te gebruiken", "default": "Hoe te gebruiken" }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Voordeel",
      "limit": 8,
      "settings": [
        { "type": "text", "id": "text", "label": "Voordeel tekst", "default": "Voordeel" }
      ]
    },
    {
      "type": "avatar",
      "name": "Klantfoto",
      "settings": [
        { "type": "image_picker", "id": "avatar_image", "label": "Klantfoto" },
        { "type": "text", "id": "avatar_alt", "label": "Alt tekst", "default": "Tevreden klant" }
      ]
    },
    {
      "type": "package",
      "name": "Pakket kaart",
      "settings": [
        { "type": "text", "id": "pkg_name", "label": "Pakketnaam", "default": "40 Dagen — Probeer het uit" },
        { "type": "text", "id": "pkg_day_price", "label": "Prijs per dag", "default": "Slechts €0,75 per dag" },
        { "type": "text", "id": "pkg_badge_text", "label": "Badge tekst (laat leeg = geen badge)" },
        { "type": "color", "id": "pkg_badge_bg", "label": "Badge achtergrond", "default": "#111111" },
        { "type": "color", "id": "pkg_badge_color", "label": "Badge tekstkleur", "default": "#ffffff" },
        { "type": "checkbox", "id": "pkg_highlight", "label": "Markeer als aanbevolen (oranje rand)", "default": false },
        { "type": "text", "id": "pkg_highlight_label", "label": "Aanbevolen label (bijv. Meest gekozen)", "default": "Meest gekozen" },
        { "type": "text", "id": "pkg_price", "label": "Prijs (bijv. €29,95)", "default": "€29,95" },
        { "type": "text", "id": "pkg_compare_price", "label": "Vergelijkingsprijs (laat leeg = geen)" },
        { "type": "text", "id": "pkg_variant_id", "label": "Shopify Variant ID (voor ATC)", "default": "0" },
        { "type": "image_picker", "id": "pkg_image", "label": "Productfoto in kaart" },
        { "type": "checkbox", "id": "show_extras", "label": "Toon gratis extras", "default": false }
      ]
    },
    {
      "type": "package_extra",
      "name": "Gratis extra (bij pakket)",
      "settings": [
        { "type": "range", "id": "extra_pkg_index", "min": 1, "max": 10, "step": 1, "label": "Bij pakket nummer (1 = eerste pakket)", "default": 1 },
        { "type": "image_picker", "id": "extra_image", "label": "Extra foto" },
        { "type": "text", "id": "extra_text", "label": "Extra tekst", "default": "+ Gratis Face Guide (E-book)" },
        { "type": "text", "id": "extra_old_price", "label": "Originele prijs (bijv. €14,95, laat leeg = gratis)" }
      ]
    },
    {
      "type": "guarantee",
      "name": "Garantie blok",
      "settings": [
        { "type": "image_picker", "id": "guarantee_icon_image", "label": "Badge/logo afbeelding" },
        { "type": "text", "id": "guarantee_title", "label": "Titel", "default": "30-Dagen Risicovrij Proberen" },
        { "type": "textarea", "id": "guarantee_body", "label": "Beschrijving", "default": "Niet tevreden? Stuur het product terug en krijg je geld terug." },
        { "type": "text", "id": "guarantee_sub", "label": "Sub-tekst", "default": "Minder dan 1% maakt gebruik van deze garantie" },
        { "type": "color", "id": "guarantee_bg", "label": "Achtergrond", "default": "#f9f9f7" },
        { "type": "range", "id": "guarantee_radius", "min": 0, "max": 24, "step": 2, "unit": "px", "label": "Afronding", "default": 12 }
      ]
    }
  ],
  "presets": [
    {
      "name": "MT-PDP-BL",
      "blocks": [
        { "type": "benefit", "settings": { "text": "Diepe reiniging zonder de huid uit te drogen" } },
        { "type": "benefit", "settings": { "text": "Verwijdert vuil, talg en onzuiverheden" } },
        { "type": "benefit", "settings": { "text": "Kalmeert met aloë vera en pepermunt" } },
        { "type": "benefit", "settings": { "text": "Geschikt voor dagelijks gebruik" } }
      ]
    }
  ]
}
{% endschema %}
